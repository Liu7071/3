@model IEnumerable<Core.Users>

@{
    <table class="table">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UserName</td>
                <td>@item.Password</td>
            </tr>
        }
    </table>
}


@Html.ActionLink("添加", "AddUser")